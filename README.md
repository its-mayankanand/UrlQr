# QR Generator from URL (FastAPI + Frontend)

## Features
- Enter a URL and generate a QR code instantly
- No database (v1)
- Basic HTML frontend
- FastAPI backend with clean folder structure

## Run
```bash
uvicorn app.main:app --reload
